const {query, param, body, validationResult} = require('express-validator');
const {status} = require('http-status');
const generateResponse = require('../helper/responseHandler');



module.exports = {

}